gui:
  theme:
    activeBorderColor:
      # - yellow
      # - bold
promptToReturnFromSubprocess: false
customCommands:
  - key: "I"
    command: "p=$(git -C $(dirname {{.SelectedFile.Name}}) rev-parse --show-toplevel)/{{.SelectedFile.Name}} && fish -C \"set p $p\" -c \"launch_new_tab 'yazi $p'\""
    context: "global"
    description: "Yazi"
